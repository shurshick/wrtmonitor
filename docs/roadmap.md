# Roadmap WrtMonitor

Все версии до `1.0.0` тестовые. История завершённых релизов находится в [CHANGELOG.md](../CHANGELOG.md), а не дублируется здесь.

Статусы функций:

- `code complete` - реализация находится в исходниках;
- `CI verified` - пройдены автоматические тесты;
- `hardware verified` - приложен полный отчёт физического OpenWrt;
- `beta accepted` - функция выдержала публичное тестирование.

## v0.41.0 - Clients And Devices

- единая запись клиента при смене адреса и способа подключения;
- тип устройства с автоматическим определением и ручной корректировкой;
- ограниченная история присутствия `online`, `recent` и `offline`;
- отдельные контракты детальной карточки, активности и трафика;
- одинаковая карточка клиента в Web UI и Android.

Статус: выпущен.

## v0.40.0 - Daily Router Management

- спокойная главная без необработанной telemetry и технических идентификаторов;
- быстрые действия для перезагрузки, Wi-Fi, гостевой сети, клиентов и диагностики;
- понятный журнал последних событий;
- одинаковый ежедневный сценарий в Web UI и Android.

Статус: выпущен.

## v0.39.0 - Health Monitoring

- история температуры, нагрузки, памяти и заполнения накопителя;
- единое состояние WAN, DNS, Wi-Fi, агента и ресурсов;
- важные дедуплицированные предупреждения с автоматическим восстановлением;
- одинаковый health-контракт и графики в Web UI и Android.

Статус: выпущен.

## v0.25.0 - Core Certification

- матрица 90 команд и поверхностей управления;
- обязательный fail-closed post-condition verifier;
- x86 harness для timeout, redelivery, idempotency и rollback;
- отдельный аппаратный certification gate;
- атомарный подписанный bundle агента, Docker-образ и GitHub Release;
- актуальные инструкции без старых зафиксированных ссылок.

Статус: выпущен.

## v0.26.0 - Architecture Split

- backend-модули network, Wi-Fi, firewall, VPN, system и maintenance;
- разделение agent commands и telemetry по подсистемам;
- типизированные command request/result schemas;
- Android Repository, ViewModel и DTO/domain без JSON в Compose;
- ограничение размера основных модулей и архитектурный тест.

Статус: выпущен.

## v0.27.0 - Operational Parity

- единая матрица пользовательских операций Web и Android;
- одинаковые названия, состояния, ошибки и подтверждения;
- динамические варианты выбора из capability/runtime API;
- Android emulator UI tests;
- проверка Back, сохранения экрана и восстановления сессии.

Статус: выпущен.

## После v0.27.0

## v0.28.0 - Operations And Notifications

- уведомления и эксплуатационные метрики;
- плановая очистка данных;
- диагностический архив сервера;
- canary-выдержка перед `latest`;
- исправленный источник клиентского трафика `nlbwmon`.

Статус: выпущен в canary.

## v0.29.0 - Network Policy

SQM/QoS, клиентские DNS-политики, история Multi-WAN, шаблоны Firewall/VPN и каталог прошивок.

Статус: выпущен в canary.

## v0.30.0 - OpenWrt Modules

USB, накопители, сетевые службы, принтеры и модемы с capability-driven интерфейсом.

Статус: выпущен в canary.

## v0.31.1 - Hardware Certification

- полный прогон 91 команды на Netis NX31 и OpenWrt x86;
- доказательства idempotency, timeout, redelivery, post-condition и rollback;
- исправления mesh, VPN policy и PBR, найденные аппаратными тестами;
- совместимая со старым OpenSSL подпись обновлений агента;
- воспроизводимый certification runner и опубликованные обезличенные отчёты.

Статус: `hardware verified`, тестовый релиз.

## v0.31.2 - DNS Recovery

- безопасная установка компонентов DoH/DoT без потери системного DNS;
- восстановление конфигурации `dnsmasq` после отключения или ошибки установки;
- обязательная проверка реального DNS-разрешения после команды;
- повторный аппаратный прогон на Netis NX31 и OpenWrt x86.

Статус: проверен на аппаратных стендах, тестовый hotfix.

## v0.32.0 - Push And Reliability

- long-poll доставки команд с сохранением короткого polling для старых агентов;
- единый SSE-поток для Web UI и Android;
- независимые deadlines команд, telemetry и автообновления агента;
- reconnect с backoff и резервный периодический refresh интерфейсов;
- метрики long-poll, подписчиков и потерянных событий;
- нагрузочная проверка 100 одновременно ожидающих агентов.

Статус: тестовый релиз.

## v0.35.15 - Hardware Recertification

- контракт расширен до 93 команд, включая SSH-сессию и ограниченный shell-script;
- полный повторный прогон на Netis NX31 и OpenWrt x86;
- для каждой применимой команды проверены повторная доставка, timeout,
  post-condition и восстановление исходной конфигурации;
- Netis NX31: 89 PASS, 4 `not_applicable`, 0 ошибок;
- OpenWrt x86: 88 PASS, 5 `not_applicable`, 0 ошибок.

Статус: `hardware verified`, тестовый релиз.

## v0.35.16 - Durable Web Terminal

- локально встроенный xterm без внешнего CDN;
- отдельные UUID сессий и постоянный PostgreSQL-брокер кадров;
- настоящий PTY OpenWrt с resize и восстановлением транспортных потоков;
- автоматическое завершение и очистка сессий;
- PostgreSQL, Chromium и аппаратный terminal E2E.

Статус: `hardware verified on OpenWrt x86`, тестовый релиз.

## v0.35.17 - Terminal Transport Hotfix

- подтверждение команды только после готовности PTY и двух транспортных направлений;
- конечные HTTPS-порции вывода без разрыва читающего дескриптора FIFO;
- совместимый запуск на старом и новом BusyBox;
- аппаратный browser-to-PTY E2E через публичный сервер.

Статус: `hardware verified on OpenWrt x86`, тестовый релиз.

## v0.36.0 - Events And Automation

- единый постоянный журнал событий сервера и роутеров;
- подтверждение, отложенный просмотр, объединение повторов и автоматическая очистка;
- правила уведомлений для in-app, webhook, ntfy и SMTP;
- безопасные сценарии с шаблонами, dry-run, cooldown и лимитом запусков;
- фактический результат команды в истории автоматизации;
- запрет циклических и критичных автоматических действий.

Статус: тестовый релиз.

## v0.37.0 - Hardware Identity

- универсальная аппаратная идентификация Device Tree/OpenWrt;
- каталог моделей и SoC в PostgreSQL без подмены observed telemetry;
- частоты CPU и перечисление всех thermal/hwmon датчиков;
- температурная история и одинаковое отображение в Web UI и Android;
- первый проверяемый профиль Netis NX31 / MediaTek MT7981B.

Статус: тестовый релиз.

## v0.38.0 - Hardware Intelligence

- автоматическое изучение неизвестных плат по точной наблюдаемой идентичности;
- проверенные профили распространённых MediaTek, Qualcomm и Rockchip устройств;
- нормализация дублирующихся датчиков одного аппаратного узла;
- реальные thermal/hwmon пороги, температурное состояние и доступный ядру throttling;
- отдельные аппаратные экраны Web UI и Android без расчётных значений.

Статус: тестовый релиз.

## v0.38.1 - Hardware Certification

- физическая сверка Netis NX31 и OpenWrt x86;
- полный набор thermal trip points и честное состояние `unsupported`;
- экспорт аппаратного отчёта без секретов;
- проверка автоматического изучения неизвестной платы.

Статус: аппаратно проверенный тестовый релиз.
